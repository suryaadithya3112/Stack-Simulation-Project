# Stack-Simulation-Project
This C program simulates a stack using an array. It allows users to push, pop, peek, and display stack elements with overflow and underflow checks. The program takes a user-defined stack size and provides a menu-driven interface to perform stack operations interactively.
